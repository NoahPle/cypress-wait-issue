# cypress-wait-issue
shows the issue on cypress wait timeouts

In cypress the wait function is significantly slower on most CIs than on local machines, which causes timeout errors for tests that don't fail locally

